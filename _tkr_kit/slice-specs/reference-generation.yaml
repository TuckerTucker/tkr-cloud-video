capability: reference-generation
slice_specs:
  - name: reference-conditioned-release-1-spec
    title: Reference-conditioned workflow graph Specification
    status: planned
    format: full
    capability: reference-generation
    feature: reference-conditioned-release
    slice: 1
    source_story_ids:
      - reference-generation/story/publish-a-reference-conditioned-workflow
    purpose: Give a staged reference somewhere to bind. The binder mutates only declared node inputs
      and refuses a binding whose input name is absent from the pinned graph, so an image cannot be
      bound into a node that has no image input. The currently published graph drives the image-conditioning
      node with nothing attached, which is what text-to-video is on this model set; this slice publishes
      a graph where the reference inputs exist.
    layer_assignment:
      - component: reference workflow graph
        layer: data
        responsibility: A ComfyUI API workflow whose reference inputs are declared
      - component: binding map
        layer: data
        responsibility: Request sources mapped to exact node inputs, drawn from the existing allowlist
    interfaces:
      - name: workflow-api.json
        kind: artifact
        signature: ComfyUI API-format graph including reference loader nodes feeding the conditioning
          node
        async: false
        ownership: Immutable once published and addressed by digest; a change is a new revision, never
          an edit.
      - name: catalog.json workflow.bindings
        kind: artifact
        signature: list of {source, node_id, input_name} entries
        async: false
        ownership: Each source must be a member of the release binding allowlist, which already carries
          exactly four input slots, matching the contract's maximum of four references.
    data_models:
      - name: WorkflowBinding
        shape: existing release model
        fields: source; node_id; input_name
        validation: source is validated against the allowlist; node identifiers reject path and control
          syntax
    behavior:
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: a graph whose reference input is absent
        when: a reference request is bound
        then: binding fails naming the missing input, rather than generating without the reference
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: a binding map naming a source outside the allowlist
        when: the manifest is rendered
        then: publication fails, because the binding vocabulary is release-approved rather than free
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: the pinned ComfyUI revision
        when: the node signature is read
        then: the declared input names are recorded beside the graph so a reader can re-check them
    error_handling:
      - condition: binding_input_missing
        error: WorkflowBindingError
        recovery: Fail before prompt submission; a graph that cannot receive a reference must not run
          as though it had
        retryable: false
      - condition: binding_source_not_allowlisted
        error: ValidationError
        recovery: Refuse the catalog at render time
        retryable: false
    dependencies:
      required_slice_ids: []
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the pinned ComfyUI revision's node signature
        - the existing binding allowlist
      provides:
        - a graph a reference request can bind into
        - a release-approved binding map
      documented_routes: []
      reference_documentation:
        - The pinned ComfyUI revision is recorded in the worker image build arguments; read the node
          signature there rather than restating it here.
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.reference-conditioned-release.1.started
        - reference-generation.reference-conditioned-release.1.succeeded
        - reference-generation.reference-conditioned-release.1.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - The graph is immutable and digest-addressed; a revision is published, never edited in place.
      - No new binding source is introduced, so the release binding vocabulary is unchanged.
    test_scenarios:
      - id: S1-T01
        level: unit
        given: a graph missing the reference input
        when: a reference request is bound
        then: binding fails naming the input
      - id: S1-T02
        level: unit
        given: four references and four declared inputs
        when: binding runs
        then: each reaches its declared node input
      - id: S1-T03
        level: unit
        given: a binding source outside the allowlist
        when: the catalog is validated
        then: it is refused
      - id: S1-TF
        level: failure_injection
        given: a graph whose digest does not match the manifest
        when: binding runs
        then: it is refused before submission
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: exempt
      detail: Introduces no store. The graph, binding map and envelope table are versioned in git and
        shipped in the pinned image; the published manifest is immutable and digest-addressed under
        the existing artifact layout.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: reference-conditioned-release-2-spec
    title: Reference model-set envelope Specification
    status: planned
    format: full
    capability: reference-generation
    feature: reference-conditioned-release
    slice: 2
    source_story_ids:
      - reference-generation/story/register-the-reference-model-set-envelope
    purpose: Let a reference request resolve its own dimensions. Envelopes are keyed by model-set identity
      precisely so a second model set cannot inherit the first one's numbers, and an unregistered model
      set is refused rather than guessed at, so a reference model set is unusable until it is registered
      here.
    layer_assignment:
      - component: TrainedRange entry
        layer: data
        responsibility: The canvas and frame range the reference model set was trained on
      - component: envelope digest
        layer: data
        responsibility: Restated with the table, verified at import
    interfaces:
      - name: TRAINED_RANGES
        kind: registry
        signature: mapping of model-set identity to TrainedRange
        async: false
        ownership: 'Keyed on the full identifier deliberately: a rebuild mints a new identifier and
          someone has to affirm the envelope still holds rather than inherit it by prefix.'
      - name: ENVELOPE_DIGEST
        kind: integrity_constant
        signature: the digest the tables must reproduce at import
        async: false
        ownership: Restating it is the act that records a deliberate change to where default requests
          land.
    data_models:
      - name: TrainedRange
        shape: existing frozen dataclass
        fields: model_set_id; short_edge; long_edge; min_frames; max_frames; grid_stride; grid_offset;
          canvas_multiple; source
        validation: construction rejects an envelope that cannot describe a reachable request
    behavior:
      - story_id: reference-generation/story/register-the-reference-model-set-envelope
        given: a reference request naming an unregistered model set
        when: intake runs
        then: it is refused naming the model set rather than given another model set's numbers
      - story_id: reference-generation/story/register-the-reference-model-set-envelope
        given: an edited range with the digest unchanged
        when: the tables load
        then: the import fails, because the recorded revision would no longer describe where defaults
          land
      - story_id: reference-generation/story/register-the-reference-model-set-envelope
        given: a reference request below the trained floor
        when: intake runs
        then: it is permitted and reported as a departure, matching the existing rule that the ceiling
          bounds and the floor reports
    error_handling:
      - condition: no_registered_envelope
        error: ValueError
        recovery: Refuse the request; a guessed default would misdescribe the run
        retryable: false
      - condition: envelope_digest_mismatch
        error: TrainedEnvelopeIntegrityError
        recovery: Fail at import rather than serve a silently changed default
        retryable: false
    dependencies:
      required_slice_ids: []
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the pinned envelope registry
      provides:
        - default resolution for reference requests
        - bounds enforcement for the reference model set
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.reference-conditioned-release.2.started
        - reference-generation.reference-conditioned-release.2.succeeded
        - reference-generation.reference-conditioned-release.2.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - The model-set identity decision is recorded, since the currently published identity names text-to-video
        while shipping the keyframe checkpoint.
    test_scenarios:
      - id: S2-T01
        level: unit
        given: an unregistered model set
        when: intake runs
        then: the request is refused naming it
      - id: S2-T02
        level: unit
        given: a registered reference model set with no dimensions given
        when: intake runs
        then: its trained dimensions are filled
      - id: S2-T03
        level: unit
        given: an edited table with a stale digest
        when: the module is imported
        then: the import fails
      - id: S2-TF
        level: failure_injection
        given: a canvas off the multiple and frames off the grid
        when: intake runs
        then: each is refused naming its own rule
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: exempt
      detail: Introduces no store. The graph, binding map and envelope table are versioned in git and
        shipped in the pinned image; the published manifest is immutable and digest-addressed under
        the existing artifact layout.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: reference-conditioned-release-3-spec
    title: Reference release approval and publication Specification
    status: planned
    format: full
    capability: reference-generation
    feature: reference-conditioned-release
    slice: 3
    source_story_ids:
      - reference-generation/story/publish-a-reference-conditioned-workflow
    purpose: Publish the reference release under an approval that actually covers it. The reviewer
      approval binds a manifest digest, and republishing a workflow changes that digest, so a reference
      release without a re-signature is a release the gate must refuse. This slice exists because that
      consequence is easy to discover late and expensive to discover at run time.
    layer_assignment:
      - component: release manifest
        layer: data
        responsibility: The rendered manifest for the reference catalog
      - component: license approval
        layer: data
        responsibility: A reviewer approval bound to the new manifest digest
      - component: publication path
        layer: service
        responsibility: The existing publish flow, workflow last
    interfaces:
      - name: prepare/publish release
        kind: service
        signature: the existing release commands, given the reference catalog and approval identifier
        async: false
        ownership: Publishes the workflow last, so the release resolves only after every artifact it
          needs verifies.
      - name: release gate
        kind: service
        signature: compares approval model set and manifest digest against the rendered manifest
        async: false
        ownership: Reused unchanged; the gate is what makes an uncovered digest a publication failure
          rather than a run-time surprise.
    data_models:
      - name: license approval
        shape: existing model
        fields: approval_id; reviewer_id; model_set_id; manifest_digest; deployment_use; territories;
          approved_at; expires_at; constraints
        validation: the digest must equal the rendered manifest's; territory and expiry are carried
          forward rather than widened
    behavior:
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: a republished workflow
        when: the manifest digest is computed
        then: it differs from the current one, so the existing approval no longer covers it
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: an approval bound to a different digest
        when: publication runs
        then: the gate refuses, rather than the mismatch surfacing when a worker executes
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: an expired approval
        when: publication runs
        then: it is refused
    error_handling:
      - condition: approval_digest_mismatch
        error: release gate failure
        recovery: Refuse publication and require a reviewer re-signature
        retryable: false
      - condition: approval_expired
        error: release gate failure
        recovery: Refuse; an expired approval is not a weaker approval
        retryable: false
    dependencies:
      required_slice_ids:
        - 1
        - 2
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the reference catalog
        - a reviewer license approval
      provides:
        - a published, digest-addressed reference release
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.reference-conditioned-release.3.started
        - reference-generation.reference-conditioned-release.3.succeeded
        - reference-generation.reference-conditioned-release.3.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - Territory and deployment use are carried forward from the existing approval, never widened
        as a side effect of a workflow change.
      - The approval is human-signed; nothing in this slice may mint one.
    test_scenarios:
      - id: S3-T01
        level: unit
        given: a manifest digest not matching the approval
        when: the gate runs
        then: publication is refused
      - id: S3-T02
        level: unit
        given: an expired approval
        when: the gate runs
        then: publication is refused
      - id: S3-T03
        level: integration
        given: a valid approval and catalog
        when: publication runs
        then: the workflow is published last
      - id: S3-TF
        level: failure_injection
        given: a partial publication
        when: it is retried
        then: no consumer-visible release exists for an unverified artifact set
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: exempt
      detail: Introduces no store. The graph, binding map and envelope table are versioned in git and
        shipped in the pinned image; the published manifest is immutable and digest-addressed under
        the existing artifact layout.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: reference-conditioned-release-4-spec
    title: Wire Reference-Conditioned Release Specification
    status: planned
    format: full
    capability: reference-generation
    feature: reference-conditioned-release
    slice: 4
    source_story_ids:
      - reference-generation/story/publish-a-reference-conditioned-workflow
      - reference-generation/story/register-the-reference-model-set-envelope
    purpose: Make the mode reachable. The request type, the stager and the prompt sections already
      exist, so wiring is mostly a matter of the model set becoming selectable and the surface offering
      the mode with its own dimensions and prompt shape.
    layer_assignment:
      - component: intake
        layer: integration
        responsibility: Offer the reference model set and its resolved bounds
      - component: console form
        layer: integration
        responsibility: Offer the mode, its references and its prompt sections
    interfaces:
      - name: form_constraints
        kind: integration_function
        signature: extended to describe the reference model set
        async: false
        ownership: Serves the registry rather than restating it, as it already does for the text-only
          model set.
      - name: console reference form
        kind: ui_surface
        signature: mode selector, reference fields bounded at four, Ref2VA prompt sections
        async: false
        ownership: Refusals render through the existing field-and-rule defect path.
    data_models:
      - name: ReferenceToVideoRequest
        shape: existing contract model
        fields: 'mode; references: tuple[InputReference, ...]; the shared base fields'
        validation: one to four references; each reference is a validated object key with an optional
          digest
    behavior:
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: a reference request with four references
        when: it is bound
        then: each reaches its declared node input
      - story_id: reference-generation/story/register-the-reference-model-set-envelope
        given: the reference model set
        when: the form is built
        then: its trained dimensions and grid are shown from the registry
    error_handling:
      - condition: mode_unavailable
        error: Rejection
        recovery: Refuse a mode whose workflow or envelope is not published, naming which
        retryable: false
    dependencies:
      required_slice_ids:
        - 3
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the published reference release
        - the registered envelope
      provides:
        - a selectable reference mode at intake and on the console
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.reference-conditioned-release.4.started
        - reference-generation.reference-conditioned-release.4.succeeded
        - reference-generation.reference-conditioned-release.4.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
    test_scenarios:
      - id: S4-T01
        level: integration
        given: a reference request naming the published model set
        when: intake runs
        then: it is accepted with resolved dimensions
      - id: S4-T02
        level: integration
        given: the console form
        when: it is built
        then: the reference mode and its bounds come from the registry
      - id: S4-TF
        level: failure_injection
        given: a mode whose workflow is unpublished
        when: it is selected
        then: it is refused naming what is missing
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: exempt
      detail: Introduces no store. The graph, binding map and envelope table are versioned in git and
        shipped in the pinned image; the published manifest is immutable and digest-addressed under
        the existing artifact layout.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: reference-conditioned-release-5-spec
    title: Test Reference-Conditioned Release Specification
    status: planned
    format: full
    capability: reference-generation
    feature: reference-conditioned-release
    slice: 5
    source_story_ids:
      - reference-generation/story/publish-a-reference-conditioned-workflow
      - reference-generation/story/register-the-reference-model-set-envelope
    purpose: 'Prove the two things that would otherwise fail late: that four references reach four
      declared node inputs, and that an uncovered manifest digest fails publication rather than a run.
      A live generation is included because nothing has ever executed this graph.'
    layer_assignment:
      - component: binding tests
        layer: integration
        responsibility: Four references into four declared inputs
      - component: gate tests
        layer: integration
        responsibility: An uncovered digest fails publication
      - component: live validation
        layer: integration
        responsibility: One reference generation to a committed result
    interfaces:
      - name: live run evidence
        kind: test_artifact
        signature: a committed result for one reference generation
        async: false
        ownership: Recorded as run evidence on the slice, because the graph has no execution history.
    data_models:
      - name: reference fixture
        shape: test double
        fields: one to four object keys with declared digests
        validation: fixtures are built in a per-test temporary directory removed by the runner
    behavior:
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: a graph missing one declared input
        when: binding runs
        then: it fails naming that input
      - story_id: reference-generation/story/register-the-reference-model-set-envelope
        given: an unregistered model set
        when: intake runs
        then: it is refused rather than defaulted
      - story_id: reference-generation/story/publish-a-reference-conditioned-workflow
        given: an approval not covering the manifest
        when: publication runs
        then: it is refused
    error_handling:
      - condition: live_generation_failure
        error: test failure
        recovery: Treat as a blocker for the slice; the graph is unproven until one run commits
        retryable: false
    dependencies:
      required_slice_ids:
        - 4
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the published release
      provides:
        - evidence the reference path binds and executes
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.reference-conditioned-release.5.started
        - reference-generation.reference-conditioned-release.5.succeeded
        - reference-generation.reference-conditioned-release.5.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
    test_scenarios:
      - id: S5-T01
        level: unit
        given: four references
        when: binding runs
        then: each reaches its declared input
      - id: S5-T02
        level: unit
        given: a graph missing an input
        when: binding runs
        then: it fails naming it
      - id: S5-T03
        level: unit
        given: an uncovered manifest digest
        when: publication runs
        then: it is refused
      - id: S5-T04
        level: integration
        given: one reference generation
        when: it runs on the endpoint
        then: a result marker commits and the video verifies
      - id: S5-TF
        level: failure_injection
        given: each declared condition
        when: the path runs
        then: the recovery contract holds
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: exempt
      detail: Introduces no store. The graph, binding map and envelope table are versioned in git and
        shipped in the pinned image; the published manifest is immutable and digest-addressed under
        the existing artifact layout.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: authorized-reference-intake-6-spec
    title: Cross-border reference-image assessment Specification
    status: planned
    format: full
    capability: reference-generation
    feature: authorized-reference-intake
    slice: 6
    source_story_ids:
      - reference-generation/story/assess-cross-border-reference-processing
    purpose: Record the assessment the provision requires before the processing it governs, not after.
      An uploaded reference image can depict an identifiable individual and the output is a moving
      likeness derived from it; worker placement already spans six territories, so enabling an intake
      path without this record would convert a noted finding into a live one.
    layer_assignment:
      - component: assurance record
        layer: data
        responsibility: A dated, scoped review reaching a verdict on reference-image processing
    interfaces:
      - name: assessment record
        kind: document
        signature: scope reviewed, sensitivity, purpose, territories, protection measures, verdict,
          residual risks accepted and by whom
        async: false
        ownership: Append-only. Renewal when territories or measures change is a new dated record,
          never an edit to a verdict.
    data_models:
      - name: assessment record
        shape: markdown document
        fields: Date; Scope; the personal information involved; the territories; the measures; the
          verdict; accepted residual risk
        validation: a verdict that is anything but a clean pass must name what was accepted and by
          whom; a review recording no accepted risk either found none or did not look
    behavior:
      - story_id: reference-generation/story/assess-cross-border-reference-processing
        given: no recorded assessment
        when: an intake path would be enabled
        then: the dependent slices stay blocked, because the assessment precedes the processing rather
          than describing it afterwards
      - story_id: reference-generation/story/assess-cross-border-reference-processing
        given: a recorded assessment
        when: territories or measures later change
        then: renewal is a new dated record rather than an edit to the existing verdict
      - story_id: reference-generation/story/assess-cross-border-reference-processing
        given: measures that are asserted rather than enforced
        when: the record is written
        then: it says which are which, so the record does not read stronger than the system
    error_handling:
      - condition: assessment_absent
        error: execution gate
        recovery: Keep the dependent slices blocked; this is the gate, not a document task
        retryable: false
      - condition: assessment_stale
        error: execution gate
        recovery: Require a new dated record when what it reviewed has changed
        retryable: false
    dependencies:
      required_slice_ids: []
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the deployed worker placement and its approved territories
        - the credential model and the declared retention position
      provides:
        - a recorded verdict on reference-image processing outside Quebec
      documented_routes: []
      reference_documentation:
        - The placement decision and its territories are recorded in the architecture decision record;
          the personal-information surface is recorded in the compliance triage record.
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.authorized-reference-intake.6.assessment_recorded
      required_fields:
        - event
        - outcome
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - The record names measures as enforced or asserted, matching how the triage record already distinguishes
        the one field whose safeguard is enforced from those that are declared.
      - No reference image is staged for a worker read before this record exists and is accepted.
    test_scenarios:
      - id: S6-T01
        level: review
        given: the drafted record
        when: it is reviewed
        then: it names scope, sensitivity, purpose, territories, measures and a verdict
      - id: S6-T02
        level: review
        given: a qualified verdict
        when: it is reviewed
        then: the residual risk and its acceptor are named
      - id: S6-T03
        level: review
        given: a later change of territory or measure
        when: renewal is considered
        then: a new dated record is produced
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - The verdict is a human's; the evidence may be gathered but the acceptance is signed.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: exempt
      detail: Introduces no store. The record is an append-only assurance document; renewal is a new
        dated file rather than an edit to an existing verdict.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: authorized-reference-intake-7-spec
    title: Principal-scoped input namespace Specification
    status: planned
    format: full
    capability: reference-generation
    feature: authorized-reference-intake
    slice: 7
    source_story_ids:
      - reference-generation/story/refuse-a-reference-a-principal-does-not-own
      - reference-generation/story/supply-one-to-four-reference-images
    purpose: Make input authorization derived rather than looked up. If an input's key carries the
      principal that owns it, authorization is a property of the key and no table can disagree with
      it, which is the same reasoning that already makes job identity a digest over the principal rather
      than a stored association.
    layer_assignment:
      - component: input namespace
        layer: domain
        responsibility: An inputs prefix scoped by principal
      - component: InputSource.authorized
        layer: domain
        responsibility: Derive ownership from the key rather than consult a table
    interfaces:
      - name: input key grammar
        kind: domain_function
        signature: 'def input_namespace(principal_id: str) -> str'
        async: false
        ownership: Returns the one namespace a principal may read and write; a key outside it is another
          principal's.
      - name: InputSource.authorized
        kind: port_method
        signature: 'async def authorized(self, principal_id: str, key: ObjectKey) -> bool'
        async: true
        ownership: Reused unchanged as a port; this slice changes what makes it true, not its shape.
    data_models:
      - name: scoped input key
        shape: validated value object
        fields: the inputs prefix, the principal segment, then a server-owned object name
        validation: the principal segment cannot be spoofed by a crafted key; separators, traversal
          and control characters are refused by the existing key validation
    behavior:
      - story_id: reference-generation/story/refuse-a-reference-a-principal-does-not-own
        given: a key in another principal's namespace
        when: staging runs
        then: it is refused before any download, so no bytes are transferred
      - story_id: reference-generation/story/refuse-a-reference-a-principal-does-not-own
        given: an unauthorized key and an absent object
        when: each is requested
        then: the answers are indistinguishable
      - story_id: reference-generation/story/supply-one-to-four-reference-images
        given: a key in the caller's own namespace
        when: staging runs
        then: it is authorized and downloaded within its bound
    error_handling:
      - condition: input_unauthorized
        error: InputStagingError
        recovery: Refuse before download; give the same answer as absence
        retryable: false
      - condition: input_key_malformed
        error: BoundaryValidationError
        recovery: Refuse a key that could escape its namespace
        retryable: false
    dependencies:
      required_slice_ids: []
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the existing input staging port and key validation
      provides:
        - structural ownership of an input object
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.authorized-reference-intake.7.started
        - reference-generation.authorized-reference-intake.7.succeeded
        - reference-generation.authorized-reference-intake.7.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - Authorization is derived from the key, so an input cannot be reached by guessing another principal's
        object name.
      - Refusal and absence are one outcome, so the namespace cannot be probed.
    test_scenarios:
      - id: S7-T01
        level: unit
        given: a key in another principal's namespace
        when: staging runs
        then: it is refused before download
      - id: S7-T02
        level: unit
        given: an unauthorized key and an absent object
        when: both are requested
        then: the outcomes are equal
      - id: S7-T03
        level: unit
        given: a key with traversal or control syntax
        when: it is validated
        then: it is refused
      - id: S7-TF
        level: failure_injection
        given: a crafted key imitating a principal segment
        when: it is validated
        then: it is refused
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: bounded
      detail: Creates objects under the inputs prefix, which already carries a declared retention class
        and a bucket lifecycle rule. Enforcing that period belongs to the enforced-retention feature;
        what must not happen here is creating input objects outside the prefix those rules reach.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: authorized-reference-intake-8-spec
    title: Input-writing credential role Specification
    status: planned
    format: full
    capability: reference-generation
    feature: authorized-reference-intake
    slice: 8
    source_story_ids:
      - reference-generation/story/refuse-a-reference-a-principal-does-not-own
    purpose: 'Model the credential that puts an image into the input namespace, because today none
      exists: the input role holds list and read only, and the request contract takes a key that must
      already exist. Writing inputs is therefore out of band and unmodelled, and this slice either
      models it narrowly or records the decision to keep it out of band.'
    layer_assignment:
      - component: CredentialRole.INPUT_WRITER
        layer: service
        responsibility: List and write on the principal-scoped inputs namespace only
      - component: process secret allowlist
        layer: integration
        responsibility: Absent from every worker process role
    interfaces:
      - name: ROLE_CAPABILITIES
        kind: registry
        signature: role to storage capability set
        async: false
        ownership: The new role holds list and write, never delete and never share, so it cannot remove
          or publish what it wrote.
      - name: SECRET_VARIABLES
        kind: registry
        signature: process role to permitted secret variables
        async: false
        ownership: The input-writing variables appear in no worker process role, so no worker subprocess
          can be handed a credential that writes inputs.
    data_models:
      - name: input-writer credential
        shape: provisioned application key
        fields: one bucket, one prefix, list and write capabilities
        validation: single-prefix, matching the existing topology in which every credential maps to
          exactly one namespace
    behavior:
      - story_id: reference-generation/story/refuse-a-reference-a-principal-does-not-own
        given: the input-writing credential
        when: its capabilities are inspected
        then: it can write the inputs namespace and nothing else, and holds neither delete nor share
      - story_id: reference-generation/story/refuse-a-reference-a-principal-does-not-own
        given: every worker process role
        when: its secret allowlist is inspected
        then: the input-writing variables are absent from all of them
    error_handling:
      - condition: role_capability_widened
        error: test failure
        recovery: Fail the least-privilege contract rather than ship a broader key
        retryable: false
      - condition: worker_granted_input_write
        error: test failure
        recovery: Fail; a worker that can write inputs can rewrite its own conditioning
        retryable: false
    dependencies:
      required_slice_ids:
        - 7
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the existing credential topology and process secret isolation
      provides:
        - a narrow credential able to create an input object
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.authorized-reference-intake.8.started
        - reference-generation.authorized-reference-intake.8.succeeded
        - reference-generation.authorized-reference-intake.8.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - The role holds neither delete nor share, so it can neither remove what it wrote nor issue a
        link to it.
      - It is a control-plane or console credential, never a worker one.
    test_scenarios:
      - id: S8-T01
        level: unit
        given: the new role
        when: its capabilities are read
        then: list and write only, on one prefix
      - id: S8-T02
        level: unit
        given: every worker process role
        when: its allowlist is read
        then: the input-writing variables are absent
      - id: S8-T03
        level: unit
        given: a write outside the namespace
        when: it is attempted
        then: the credential cannot reach it
      - id: S8-TF
        level: failure_injection
        given: a role definition widened in place
        when: the contract test runs
        then: it fails
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - If the decision is to keep staging out of band, this slice closes as deferred with that decision
        recorded and slice 9 accepts an existing key instead.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: bounded
      detail: Creates objects under the inputs prefix, which already carries a declared retention class
        and a bucket lifecycle rule. Enforcing that period belongs to the enforced-retention feature;
        what must not happen here is creating input objects outside the prefix those rules reach.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: authorized-reference-intake-9-spec
    title: Wire Authorized Reference Intake Specification
    status: planned
    format: full
    capability: reference-generation
    feature: authorized-reference-intake
    slice: 9
    source_story_ids:
      - reference-generation/story/supply-one-to-four-reference-images
    purpose: Give the operator a way to supply references, and be explicit that doing so widens what
      the console is. Until now it composes no write path at all, and that absence is a tested property
      recorded as one of its constraints; this slice changes it, so it changes the record too rather
      than quietly invalidating it.
    layer_assignment:
      - component: console upload path
        layer: integration
        responsibility: Accept references, bounded at four, and place them in the namespace
      - component: console routes
        layer: integration
        responsibility: The reference routes and their refusals
      - component: model record
        layer: integration
        responsibility: The surface's constraints, updated where the write capability is added
    interfaces:
      - name: upload route
        kind: api_route
        signature: POST /api/inputs -> {object_key, sha256}
        async: true
        ownership: Returns the key the caller then names in a request, so the request contract is unchanged.
      - name: reference form
        kind: ui_surface
        signature: up to four references with their refusals by field and rule
        async: false
        ownership: Bounded at four in the surface as well as the contract, so the limit is visible
          before submission.
    data_models:
      - name: staged reference response
        shape: serialized mapping
        fields: 'object_key: str; sha256: str'
        validation: returns the server-owned key and the computed digest, so the caller names neither
    behavior:
      - story_id: reference-generation/story/supply-one-to-four-reference-images
        given: a supplied reference
        when: it is uploaded
        then: it lands in the caller's own namespace under a server-owned name
      - story_id: reference-generation/story/supply-one-to-four-reference-images
        given: five references
        when: they are supplied
        then: the fifth is refused before submission
      - story_id: reference-generation/story/supply-one-to-four-reference-images
        given: an uploaded reference
        when: a generation is submitted
        then: each staged reference binds into the published graph
    error_handling:
      - condition: input_write_failed
        error: AppError
        recovery: Report as an error document; the page renders every failure the same way
        retryable: true
      - condition: too_many_references
        error: Rejection
        recovery: Refuse at the surface as well as the contract
        retryable: false
    dependencies:
      required_slice_ids:
        - 5
        - 8
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the input-writing credential
        - the published reference release
      provides:
        - a reference supplied and generated from through the console
      documented_routes:
        - POST /api/inputs
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.authorized-reference-intake.9.started
        - reference-generation.authorized-reference-intake.9.succeeded
        - reference-generation.authorized-reference-intake.9.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - The console gains a write capability it previously did not have; the model record states that
        plainly rather than leaving a stale claim that it composes no write path.
      - Uploaded objects are named by the server, so a caller-supplied filename never becomes a key.
      - The surface remains loopback-only; an upload field makes publishing more tempting and no less
        out of scope.
    test_scenarios:
      - id: S9-T01
        level: integration
        given: one reference
        when: it is uploaded and a generation submitted
        then: it binds and the run reaches a committed result
      - id: S9-T02
        level: integration
        given: five references
        when: they are supplied
        then: the surface refuses before submission
      - id: S9-T03
        level: integration
        given: a caller-supplied filename
        when: an upload runs
        then: the stored key is server-owned
      - id: S9-TF
        level: failure_injection
        given: a write failure
        when: an upload runs
        then: it renders as an error document
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: bounded
      detail: Creates objects under the inputs prefix, which already carries a declared retention class
        and a bucket lifecycle rule. Enforcing that period belongs to the enforced-retention feature;
        what must not happen here is creating input objects outside the prefix those rules reach.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: authorized-reference-intake-10-spec
    title: Test Authorized Reference Intake Specification
    status: planned
    format: full
    capability: reference-generation
    feature: authorized-reference-intake
    slice: 10
    source_story_ids:
      - reference-generation/story/supply-one-to-four-reference-images
      - reference-generation/story/refuse-a-reference-a-principal-does-not-own
    purpose: 'Prove the refusals, since every one of them is what keeps a likeness from reaching a
      GPU it should not. The least-privilege assertion matters most: a credential that writes inputs
      and reaches a worker would let a worker rewrite its own conditioning.'
    layer_assignment:
      - component: staging tests
        layer: integration
        responsibility: Bounds, digest and media type refusals
      - component: authorization tests
        layer: integration
        responsibility: Namespace refusal and non-enumeration
      - component: least-privilege tests
        layer: integration
        responsibility: The write credential's reach and absence from worker roles
    interfaces:
      - name: in-memory input source
        kind: test double
        signature: authorized keys and their bytes
        async: false
        ownership: Records what was authorized and downloaded, so a test can assert that refusal happened
          before transfer.
    data_models:
      - name: reference fixtures
        shape: test doubles
        fields: keys, bytes, digests and media types
        validation: built in a per-test temporary directory removed by the runner
    behavior:
      - story_id: reference-generation/story/supply-one-to-four-reference-images
        given: five references
        when: the request is parsed
        then: it is refused on the contract's bound
      - story_id: reference-generation/story/supply-one-to-four-reference-images
        given: a reference whose bytes do not match its declared digest
        when: staging runs
        then: it is refused and nothing partial is promoted
      - story_id: reference-generation/story/supply-one-to-four-reference-images
        given: a reference that is not a permitted media type
        when: staging runs
        then: it is refused before a workflow sees it
      - story_id: reference-generation/story/refuse-a-reference-a-principal-does-not-own
        given: a key in another principal's namespace
        when: staging runs
        then: it is refused before download and indistinguishably from absence
    error_handling:
      - condition: refusal_after_download
        error: test failure
        recovery: Fail; refusing after transfer is not refusing
        retryable: false
      - condition: worker_role_holds_input_write
        error: test failure
        recovery: Fail the least-privilege contract
        retryable: false
    dependencies:
      required_slice_ids:
        - 9
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the intake path and the credential registries
      provides:
        - evidence that every declared refusal happens at the right boundary
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.authorized-reference-intake.10.started
        - reference-generation.authorized-reference-intake.10.succeeded
        - reference-generation.authorized-reference-intake.10.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - Tests assert the unauthorized and absent outcomes are equal, not merely both falsy.
    test_scenarios:
      - id: S10-T01
        level: unit
        given: five references
        when: parsing runs
        then: it is refused
      - id: S10-T02
        level: unit
        given: a digest mismatch
        when: staging runs
        then: it is refused and no file is promoted
      - id: S10-T03
        level: unit
        given: a disallowed media type
        when: staging runs
        then: it is refused
      - id: S10-T04
        level: unit
        given: another principal's key
        when: staging runs
        then: refusal precedes download and equals the absent outcome
      - id: S10-T05
        level: unit
        given: every worker process role
        when: its allowlist is read
        then: the input-writing credential is absent
      - id: S10-TF
        level: failure_injection
        given: each declared condition
        when: intake runs
        then: the recovery contract holds
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: bounded
      detail: Creates objects under the inputs prefix, which already carries a declared retention class
        and a bucket lifecycle rule. Enforcing that period belongs to the enforced-retention feature;
        what must not happen here is creating input objects outside the prefix those rules reach.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: audio-reference-intake-11-spec
    title: Voice-reference cross-border assessment Specification
    status: planned
    format: full
    capability: reference-generation
    feature: audio-reference-intake
    slice: 11
    source_story_ids:
      - reference-generation/story/assess-voice-reference-processing
    purpose: Record an assessment that actually covers audio. The reference-image record considered
      a likeness; an assurance verdict is scoped, and a review that did not consider material cannot
      be read as having cleared it. A supplied recording is a voice sample of an identifiable person
      and the output reproduces it, which is a different weighing from the one already made.
    layer_assignment:
      - component: assurance record
        layer: data
        responsibility: A dated, scoped review reaching a verdict on supplied audio
    interfaces:
      - name: audio assessment record
        kind: document
        signature: scope, the material considered, sensitivity, purpose, territories, measures, verdict,
          residual risk and its acceptor
        async: false
        ownership: Append-only and separate from the image record, because the two considered different
          material.
    data_models:
      - name: audio assessment record
        shape: markdown document
        fields: Date; Scope; the material considered; territories; measures; verdict; accepted residual
          risk
        validation: states which measures are enforced and which asserted, so the record does not read
          stronger than the system
    behavior:
      - story_id: reference-generation/story/assess-voice-reference-processing
        given: only the reference-image assessment
        when: an audio intake path would be enabled
        then: the dependent slices stay blocked, because that review did not consider this material
      - story_id: reference-generation/story/assess-voice-reference-processing
        given: a recorded audio assessment
        when: territories or measures later change
        then: renewal is a new dated record rather than an edit to the verdict
      - story_id: reference-generation/story/assess-voice-reference-processing
        given: a qualified verdict
        when: it is recorded
        then: the residual risk and its acceptor are named
    error_handling:
      - condition: audio_assessment_absent
        error: execution gate
        recovery: Keep slices 12 to 15 blocked; this is the gate, not a document task
        retryable: false
      - condition: assessment_scope_overread
        error: review failure
        recovery: Refuse to treat the image record as covering audio; a scoped verdict does not extend
          to material it did not consider
        retryable: false
    dependencies:
      required_slice_ids: []
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the recorded reference-image assessment
        - the deployed worker placement
      provides:
        - a recorded verdict on supplied audio references
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.audio-reference-intake.11.assessment_recorded
      required_fields:
        - event
        - outcome
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - No audio reference is staged for a worker read before this record exists and is accepted.
      - The record names the output as a reproduction of a supplied voice, not merely as generated
        audio.
    test_scenarios:
      - id: S11-T01
        level: review
        given: the drafted record
        when: it is reviewed
        then: it names the material considered, the territories, the measures and a verdict
      - id: S11-T02
        level: review
        given: the image record alone
        when: audio intake is proposed
        then: it is not accepted as covering audio
      - id: S11-T03
        level: review
        given: a later change of territory or measure
        when: renewal is considered
        then: a new dated record is produced
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - The verdict is a human's; the evidence may be gathered but the acceptance is signed.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: exempt
      detail: Introduces no store. The record is an append-only assurance document; renewal is a new
        dated file rather than an edit to an existing verdict.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: audio-reference-intake-12-spec
    title: Audio input media contract Specification
    status: planned
    format: full
    capability: reference-generation
    feature: audio-reference-intake
    slice: 12
    source_story_ids:
      - reference-generation/story/supply-an-audio-reference
    purpose: Change the shape the intake contract speaks in, which is where the audio gap actually
      lives. The inspection port returns a media type and two dimensions, the staged-input value carries
      them, and the stager requires both to be positive. An audio stream has none of the three, so
      adding an allowlist entry alone would produce a value the contract cannot express and a check
      it cannot pass.
    layer_assignment:
      - component: InputMediaInspector
        layer: domain
        responsibility: The inspection port, widened to describe a stream with no dimensions
      - component: StagedInput
        layer: domain
        responsibility: The staged value, carrying dimensions only where they exist
      - component: InputStager
        layer: domain
        responsibility: Per-kind bounds, replacing one unconditional dimension check
    interfaces:
      - name: InputMediaInspector.inspect
        kind: port_method
        signature: 'def inspect(self, path: Path) -> InputMedia'
        async: false
        ownership: Returns a described medium rather than a media type and two integers, so audio is
          expressible without inventing a width and a height for it.
      - name: InputStager.stage
        kind: domain_service_method
        signature: async def stage(self, principal_id, reference, input_root, index) -> StagedInput
        async: true
        ownership: Applies the bounds that belong to the kind it staged; an image is still refused
          without positive dimensions.
    data_models:
      - name: InputMedia
        shape: frozen dataclass
        fields: 'media_type: str; dimensions: tuple[int, int] | None; duration_seconds: float | None'
        validation: dimensions are absent for audio rather than zero, so nothing downstream can read
          a placeholder as a real measurement
      - name: StagedInput
        shape: existing frozen dataclass, widened
        fields: path; media_type; dimensions; size_bytes; sha256; duration_seconds
        validation: callers that need dimensions must handle their absence rather than receiving zeroes
    behavior:
      - story_id: reference-generation/story/supply-an-audio-reference
        given: an audio reference
        when: it is staged
        then: it is described without a width or a height
      - story_id: reference-generation/story/supply-an-audio-reference
        given: an image reference whose dimensions are absent
        when: it is staged
        then: it is still refused, because relaxing the requirement for audio must not relax it for
          images
      - story_id: reference-generation/story/supply-an-audio-reference
        given: an audio media type
        when: the allowlist is consulted
        then: it is permitted alongside the existing three
    error_handling:
      - condition: input_media_invalid
        error: InputStagingError
        recovery: Refuse a medium that satisfies no kind's bounds, before a workflow sees it
        retryable: false
      - condition: dimension_placeholder_introduced
        error: test failure
        recovery: Fail rather than let absent dimensions be represented as zero, which a later check
          would read as a measurement
        retryable: false
    dependencies:
      required_slice_ids:
        - 11
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the existing input staging port and staged-input value
      provides:
        - an intake contract that can describe audio
        - per-kind bounds for every permitted medium
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.audio-reference-intake.12.started
        - reference-generation.audio-reference-intake.12.succeeded
        - reference-generation.audio-reference-intake.12.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - Widening the contract for audio must not weaken it for images or videos; the bounds move from
        unconditional to per-kind, not from present to absent.
      - Absence is represented as absence, never as a zero a later check could accept.
    test_scenarios:
      - id: S12-T01
        level: unit
        given: an audio medium
        when: it is described
        then: dimensions are absent rather than zero
      - id: S12-T02
        level: unit
        given: an image with absent dimensions
        when: staging runs
        then: it is refused
      - id: S12-T03
        level: unit
        given: a video with positive dimensions
        when: staging runs
        then: it is accepted as before
      - id: S12-TF
        level: failure_injection
        given: a medium matching no kind
        when: staging runs
        then: it is refused before promotion
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: bounded
      detail: Creates objects under the inputs prefix, which already carries a declared retention class
        and a bucket lifecycle rule. Enforcing that period belongs to the enforced-retention feature;
        what must not happen here is creating input objects outside the prefix those rules reach.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: audio-reference-intake-13-spec
    title: Audio stream inspection and bounds Specification
    status: planned
    format: full
    capability: reference-generation
    feature: audio-reference-intake
    slice: 13
    source_story_ids:
      - reference-generation/story/supply-an-audio-reference
    purpose: Inspect an audio file as audio. The current inspector locates a video stream and derives
      the media type from its codec, so an audio-only file is refused before any allowlist is consulted;
      the refusal is correct today and wrong once audio is a permitted kind.
    layer_assignment:
      - component: FfprobeMediaInspector
        layer: integration
        responsibility: Locate an audio stream and derive its type from its own codec
      - component: audio bounds
        layer: service
        responsibility: Duration, channel count and sample rate
    interfaces:
      - name: inspect_input
        kind: integration_method
        signature: 'def inspect_input(self, path: Path) -> InputMedia'
        async: false
        ownership: Selects the stream that matches the kind being staged rather than assuming a video
          stream exists, and derives the media type from that stream's codec and the container.
      - name: audio bounds policy
        kind: domain_function
        signature: 'def check_audio_bounds(media: InputMedia) -> None'
        async: false
        ownership: Bounded by policy rather than by whatever the file declares.
    data_models:
      - name: audio medium
        shape: InputMedia instance
        fields: media_type; dimensions=None; duration_seconds; channels; sample_rate_hz
        validation: a file whose container and streams disagree with the kind it is offered as is refused
    behavior:
      - story_id: reference-generation/story/supply-an-audio-reference
        given: an audio-only file
        when: it is inspected
        then: an audio stream is located and its type derived from its own codec, rather than the file
          being refused for lacking a video stream
      - story_id: reference-generation/story/supply-an-audio-reference
        given: a video payload offered as audio
        when: it is inspected
        then: it is refused, because a declared kind is never taken on trust
      - story_id: reference-generation/story/supply-an-audio-reference
        given: audio beyond the permitted duration or channel count
        when: it is inspected
        then: it is refused before promotion
    error_handling:
      - condition: media_invalid
        error: AppError
        recovery: Refuse a file whose streams do not prove the kind it is offered as
        retryable: false
      - condition: audio_bounds_exceeded
        error: InputStagingError
        recovery: Refuse on duration, channels or sample rate before a workflow sees it
        retryable: false
    dependencies:
      required_slice_ids:
        - 12
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the staged file and the fixed inspection arguments
      provides:
        - a described and bounded audio medium
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.audio-reference-intake.13.started
        - reference-generation.audio-reference-intake.13.succeeded
        - reference-generation.audio-reference-intake.13.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - The inspector runs with fixed arguments on a server-owned path and bounds its own output, as
        it already does.
      - A declared media type is never trusted; the streams present decide the kind.
    test_scenarios:
      - id: S13-T01
        level: unit
        given: an audio-only file
        when: it is inspected
        then: it is described as audio
      - id: S13-T02
        level: unit
        given: a video file offered as audio
        when: it is inspected
        then: it is refused
      - id: S13-T03
        level: unit
        given: audio beyond the duration bound
        when: it is inspected
        then: it is refused
      - id: S13-T04
        level: unit
        given: an image and a video
        when: each is inspected
        then: both are described exactly as before
      - id: S13-TF
        level: failure_injection
        given: a truncated or malformed container
        when: it is inspected
        then: it is refused rather than partially described
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: bounded
      detail: Creates objects under the inputs prefix, which already carries a declared retention class
        and a bucket lifecycle rule. Enforcing that period belongs to the enforced-retention feature;
        what must not happen here is creating input objects outside the prefix those rules reach.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: audio-reference-intake-14-spec
    title: Wire Audio Reference Intake Specification
    status: planned
    format: full
    capability: reference-generation
    feature: audio-reference-intake
    slice: 14
    source_story_ids:
      - reference-generation/story/supply-an-audio-reference
    purpose: Make the audio half of the grammar reachable from the surface. The labels, the retention
      markers and the two audio task types already exist and are already served to the page as closed
      vocabularies; what is missing is a way to supply the object they refer to and a form that composes
      the relationship rather than leaving it to prose.
    layer_assignment:
      - component: console audio upload
        layer: integration
        responsibility: Accept an audio object into the caller's namespace
      - component: console reference form
        layer: integration
        responsibility: Audio labels, audio retention markers, audio task types
    interfaces:
      - name: upload route
        kind: api_route
        signature: POST /api/inputs, accepting an audio medium
        async: true
        ownership: Returns the server-owned key and computed digest, as it does for a visual reference.
      - name: audio reference form
        kind: ui_surface
        signature: an audio reference with its own bounds, its retention marker set, and the audio
          task types
        async: false
        ownership: The marker set offered is the audio one, because the visual markers do not apply
          to a sound.
    data_models:
      - name: staged audio response
        shape: serialized mapping
        fields: 'object_key: str; sha256: str; duration_seconds: float'
        validation: returns what was measured, so a caller composing a retention relationship is not
          guessing at length
    behavior:
      - story_id: reference-generation/story/supply-an-audio-reference
        given: an audio object
        when: it is uploaded
        then: it lands in the caller's namespace under a server-owned name
      - story_id: reference-generation/story/supply-an-audio-reference
        given: an audio reference in a prompt
        when: the form is composed
        then: the audio retention markers are offered and the visual ones are not
      - story_id: reference-generation/story/supply-an-audio-reference
        given: a staged audio reference
        when: a generation is submitted
        then: it binds through its allowlisted source
    error_handling:
      - condition: input_write_failed
        error: AppError
        recovery: Render as an error document; the page renders every failure the same way
        retryable: true
      - condition: marker_kind_mismatch
        error: Rejection
        recovery: Refuse a visual retention marker on an audio label and the reverse, naming the field
          and the rule
        retryable: false
    dependencies:
      required_slice_ids:
        - 13
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the audio intake contract
        - the published reference release
      provides:
        - an audio reference supplied and generated from through the console
      documented_routes:
        - POST /api/inputs
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.audio-reference-intake.14.started
        - reference-generation.audio-reference-intake.14.succeeded
        - reference-generation.audio-reference-intake.14.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - Uploaded objects are named by the server, so a caller-supplied filename never becomes a key.
      - The surface remains loopback-only; a voice upload field makes publishing more tempting and
        no less out of scope.
    test_scenarios:
      - id: S14-T01
        level: integration
        given: an audio object
        when: it is uploaded and a generation submitted
        then: it binds and the run reaches a committed result
      - id: S14-T02
        level: integration
        given: a visual marker on an audio label
        when: the prompt is composed
        then: it is refused by field and rule
      - id: S14-T03
        level: integration
        given: a caller-supplied filename
        when: an upload runs
        then: the stored key is server-owned
      - id: S14-TF
        level: failure_injection
        given: a write failure
        when: an upload runs
        then: it renders as an error document
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: bounded
      detail: Creates objects under the inputs prefix, which already carries a declared retention class
        and a bucket lifecycle rule. Enforcing that period belongs to the enforced-retention feature;
        what must not happen here is creating input objects outside the prefix those rules reach.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
  - name: audio-reference-intake-15-spec
    title: Test Audio Reference Intake Specification
    status: planned
    format: full
    capability: reference-generation
    feature: audio-reference-intake
    slice: 15
    source_story_ids:
      - reference-generation/story/supply-an-audio-reference
      - reference-generation/story/assess-voice-reference-processing
    purpose: Prove the widening did not weaken anything. The risk in this feature is not that audio
      fails to stage; it is that relaxing a dimension requirement for audio quietly relaxes it for
      images, or that an absent measurement is represented as a zero some later check reads as real.
    layer_assignment:
      - component: regression tests
        layer: integration
        responsibility: Images and videos are bounded exactly as before
      - component: audio tests
        layer: integration
        responsibility: Kind confusion, bounds and absent dimensions
    interfaces:
      - name: mixed reference fixture
        kind: test_fixture
        signature: one visual and one audio reference in a single request
        async: false
        ownership: Exercises the binder with two kinds at once, which is the case the allowlist's four
          slots exist for.
    data_models:
      - name: audio fixtures
        shape: test doubles
        fields: audio-only, video-offered-as-audio, out-of-bounds audio, dimensionless image
        validation: built in a per-test temporary directory removed by the runner
    behavior:
      - story_id: reference-generation/story/supply-an-audio-reference
        given: an image whose dimensions are absent
        when: staging runs
        then: it is refused, exactly as before this feature
      - story_id: reference-generation/story/supply-an-audio-reference
        given: a video payload offered as audio
        when: staging runs
        then: it is refused
      - story_id: reference-generation/story/supply-an-audio-reference
        given: one visual and one audio reference
        when: a generation is submitted
        then: both bind through their allowlisted sources
      - story_id: reference-generation/story/assess-voice-reference-processing
        given: the assurance record set
        when: it is checked
        then: a dated audio record exists distinct from the image one
    error_handling:
      - condition: dimension_requirement_regressed
        error: test failure
        recovery: Fail; the whole risk of this feature is that a relaxation meant for audio reaches
          images
        retryable: false
      - condition: placeholder_measurement_accepted
        error: test failure
        recovery: Fail; an absent measurement represented as zero would pass a positive check somewhere
          downstream
        retryable: false
    dependencies:
      required_slice_ids:
        - 14
      external:
        - Python standard library
      inversion_of_control: Every side-effecting collaborator is a Protocol dependency supplied by
        the capability composition root; tests use deterministic fakes.
    integration_points:
      consumes:
        - the widened intake path
      provides:
        - evidence that audio stages and that visual bounds are unchanged
      documented_routes: []
      reference_documentation: []
    configuration:
      - name: LOG_LEVEL
        type: enum
        default: INFO
        secret: false
        validation: DEBUG|INFO|WARNING|ERROR
    observability:
      events:
        - reference-generation.audio-reference-intake.15.started
        - reference-generation.audio-reference-intake.15.succeeded
        - reference-generation.audio-reference-intake.15.failed
      required_fields:
        - event
        - outcome
        - duration_ms
        - error_code?
      forbidden_fields:
        - credentials
        - authorization headers
        - signed URL tokens
        - full prompts
        - user-supplied filenames
    security_constraints:
      - Validate at the boundary and pass typed values inward.
      - No global clients or ambient credentials; every collaborator is injected.
      - Fail closed on configuration, authorization and digest uncertainty.
      - Regression assertions for images and videos run alongside the new audio ones, in the same suite.
    test_scenarios:
      - id: S15-T01
        level: unit
        given: an audio-only file
        when: staging runs
        then: it is staged and described without dimensions
      - id: S15-T02
        level: unit
        given: a dimensionless image
        when: staging runs
        then: it is refused
      - id: S15-T03
        level: unit
        given: a video offered as audio
        when: staging runs
        then: it is refused
      - id: S15-T04
        level: unit
        given: audio outside its duration, channel or sample-rate bounds
        when: staging runs
        then: each is refused
      - id: S15-T05
        level: integration
        given: one visual and one audio reference
        when: a generation is submitted
        then: both bind
      - id: S15-TF
        level: failure_injection
        given: each declared condition
        when: intake runs
        then: the recovery contract holds
    definition_of_done:
      - Public interfaces and models are type-checked with explicit exported return types.
      - Every behavior criterion has a discriminating automated test.
      - Every error condition has a tested recovery and retryability classification.
      - No placeholder, TODO, mutable dependency reference, or console-only error remains.
    data_lifecycle:
      kind: bounded
      detail: Creates objects under the inputs prefix, which already carries a declared retention class
        and a bucket lifecycle rule. Enforcing that period belongs to the enforced-retention feature;
        what must not happen here is creating input objects outside the prefix those rules reach.
    implementation_constraints:
      python: PEP 8, PEP 484, Google-style docstrings, async/await for I/O, explicit exception wrapping
        with domain context.
      logging: structlog only; allowlisted fields, never a credential, signed URL, prompt body or caller
        filename.
      bash: n/a
      testing: pytest with deterministic fakes for every side-effecting port.
