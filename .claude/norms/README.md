# Norms

Defeasible guidance: what SHOULD be true, overridable with a reason (unlike Rules, which bind).

- **Authoring bar:** each norm names its `exception-test:` (what justifies overriding) and a
  `reviewer:` — a norm nobody reviews is decoration.
- **Written by:** human.
- **Retention:** bounded — replace-on-write.
